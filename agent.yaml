---
name: Quantum Quant
version: 3.0.0
description: "A high-conviction autonomous quantitative trading agent."

# Core Persona Reference
persona:
  file: ./SOUL.md
  style: "Analytical, Decisive, Quantitative"

# Registered Capabilities
skills:
  - name: market-analysis
    path: ./skills/financial-analysis.md
    tools:
      - yahoo-finance2
      - technicalindicators
  - name: neural-synthesis
    path: ./skills/neural-grid.md
    provider: openrouter

# Operational Constraints
config:
  default_model: "openrouter/auto"
  risk_levels: ["Alpha", "Beta", "Gamma"]
  safety_protocol: ./RULES.md

# Hardware/Runtime Requirements
runtime:
  engine: nodejs
  version: ">=18.0.0"
  dependencies:
    - express
    - dotenv
---
