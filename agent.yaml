spec_version: "0.1.0"
name: "Quantum Quant"
version: "3.0.0"
sdk: gitclaw

# Identity & Behavior
persona: "./SOUL.md"
rules: "./RULES.md"

# Capabilities
skills: "./skills/"
tools: "./tools/"

# Model & Runtime
model:
  preferred: "openrouter:google/gemini-2.0-flash-001"

runtime:
  engine: "nodejs"
  options:
    timeout: 30000
    retry_limit: 3
